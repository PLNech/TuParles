# Third-party engines and model weights — rebuilt/redownloaded, never committed
vendor/
models/

# Python
__pycache__/
*.py[cod]
.venv/
dist/
.pytest_cache/

# Local artifacts
*.wav
*.log
.tuparles/

# Code-switch eval: generated fixtures + downloaded TTS voices.
# corpus.json is the source of truth; regenerate with
# scripts/gen_codeswitch_wavs.py — WAVs/voices/manifest are machine-local.
tests/data/codeswitch/wav/
tests/data/codeswitch/voices/

# Personal glossary — yours, not the repo's (start from vocab.example.txt)
vocab.txt

# Local-only notes / idea scratchpads — never pushed
*.local.md

# Coverage artefacts
.coverage
htmlcov/

# Meeting QA reports — real names, real quotes: never public
docs/reports/
