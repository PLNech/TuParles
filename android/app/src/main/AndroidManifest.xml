<?xml version="1.0" encoding="utf-8"?>
<!-- Structural privacy: no INTERNET permission is declared, by design. This is a
     local dictaphone — nothing leaves the device except via the user's own share
     sheet. RECORD_AUDIO is the only sensitive permission. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Hold the mic correctly while recording: a foreground service of type
         microphone (Android 14+ requirement) + its notification. No INTERNET. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TuParlesApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TuParles">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TuParles">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Compute backbone: owns recording off the UI lifecycle (a take survives
             screen-off / app-switch), foreground type=microphone while active. -->
        <service
            android:name=".record.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Shares a note's WAV to another app via a content:// URI. No INTERNET
             for us — the receiving app does any sending, the user drives it. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="pl.nech.tuparles.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
