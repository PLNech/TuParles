<?xml version="1.0" encoding="utf-8"?>
<!-- Smart widget: a one-tap dictate surface for the home screen. The whole box is
     clickable (toggles DictationService); the two lines reflect live state pushed
     by the service (idle → recording meter → decoding → ✅ copied). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#161B22"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🎙 TuParles"
        android:textColor="#E6EDF3"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Toucher pour dicter"
        android:textColor="#7D8CA0"
        android:textSize="12sp"
        android:paddingTop="2dp"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>
